[project]
name = "{{ module_name_snake }}"
version = "0.1.0"
description = "{{ module_name }} - WADUP Module"
requires-python = ">=3.11"
dependencies = []

[tool.wadup]
entry-point = "{{ module_name_snake }}"
