[package]
name = "sqlite-parser"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
wadup-guest = { path = "../../crates/wadup-guest" }
rusqlite = { version = "0.32", features = ["bundled", "wasm32-wasi-vfs"] }
